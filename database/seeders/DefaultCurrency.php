<?php

namespace Database\Seeders;

use App\Models\Currency;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class DefaultCurrency extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $currency = new Currency();
        // $currency->id = 1;
        // $currency->setTranslation('name', 'en', 'United States Dollar');
        // $currency->code = 'USD';
        // $currency->symbol = '$';
        // $currency->rate = 1;
        // $currency->save();

        // nova_set_setting_value('default_currency', $currency->id);
    }
}
